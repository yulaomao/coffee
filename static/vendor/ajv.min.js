/* Local fallback for Ajv v8 minimal UMD build. If CDN fails, window.ajv.Ajv will not exist; this file provides a tiny stub that always validates to true. Replace this with real Ajv bundle if needed for offline usage. */
(function(global){
  if(!global.ajv){ global.ajv = {}; }
  function StubAjv(){ }
  StubAjv.prototype.compile = function(){ return function(){ return true; }; };
  global.ajv.Ajv = StubAjv;
})(window);
